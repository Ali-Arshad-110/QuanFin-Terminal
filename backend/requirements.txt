# neo_api_client is optional and not available on all Python versions (e.g. 3.14).
# Core backend can run without it; broker streaming features degrade gracefully.
fastapi>=0.68.0
uvicorn>=0.15.0
yfinance>=0.1.63
pandas>=1.3.0
ccxt>=1.50.0
pydantic>=1.8.0
python-dotenv>=1.0.0
websockets>=10.0
numpy>=1.24.2
setuptools>=69.0.0

# Database
sqlalchemy>=2.0.0
aiosqlite>=0.19.0

# Scheduler & HTTP
apscheduler>=3.10.0
requests>=2.28.0
cachetools>=5.3.0

# Real-time
redis>=4.5.0
async-timeout>=4.0.0
